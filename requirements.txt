# Runtime dependencies for the deployed Streamlit app.
# Install with:  pip install -r requirements.txt
#
# Scrape-only deps (requests, beautifulsoup4, lxml) live in
# requirements-dev.txt — they are NOT needed to run the app because
# the chunks + FAISS index are prebuilt and committed under data/.

sentence-transformers==3.0.1
faiss-cpu==1.8.0
google-genai==1.0.0
rank-bm25==0.2.2
streamlit==1.35.0
python-dotenv==1.0.1
